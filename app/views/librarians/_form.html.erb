<%= form_for @librarian do |form| %>
  <% if librarian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@librairan.errors.count, "error") %> prohibited this librarian from being saved:</h2>

      <ul>
      <% @librarian.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %><br />
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %><br />
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %><br />
    <%= form.text_field :password %>
  </div>
  
  <div class="field">
    <%= form.label :library_id %>
    <%= select("librarian", "library_id", Library.all.collect{ |l| [ l.name, l.id ] }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>